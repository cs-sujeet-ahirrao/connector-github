#### Following enhancements have been made to the GitHub connector in version 2.0.0:

- Integrated support for GitHub Enterprise account.
- Removed the parameter Clone URL as one of the configuration parameters, and the cloning is now managed within the code
  for both GitHub Cloud and GitHub Enterprise.
- Moved the Server API URL parameter to the new GitHub Account parameter in the configuration to support both GitHub
  Cloud and GitHub Enterprise accounts.